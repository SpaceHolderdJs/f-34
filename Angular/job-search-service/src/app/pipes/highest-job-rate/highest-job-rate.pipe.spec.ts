import { HighestJobRatePipe } from './highest-job-rate.pipe';

describe('HighestJobRatePipe', () => {
  it('create an instance', () => {
    const pipe = new HighestJobRatePipe();
    expect(pipe).toBeTruthy();
  });
});
