import {FC} from 'react';
import { UserType } from '../types/users.types';

type Props = {
    user: UserType;
    // add remove user
}

export const UserCard: FC<Props> = () => {
    // HW: Add user layouts with data
    // Add remove user logic -> Users.tsx
  return (
    <div>UserCard</div>
  )
}
